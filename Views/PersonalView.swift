import SwiftUI

struct PersonalView: View {
    var body: some View {
        Text("Personal View")
            .font(.largeTitle)
            .padding()
            .background(Color.white)
    }
}

